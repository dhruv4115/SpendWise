# spendwise (frontend)

The Flutter client for SpendWise. Setup, environment defines and the run/test
commands are documented once, in the [repository README](../README.md).

```bash
flutter pub get
./scripts/run_dev.sh
flutter test
```
